<%= link_to edit_grid_tiles_path(tile.grid, x: tile.x, y: tile.y) do %>
  <%= image_tag(tile.terrain.image.small.url, class: 'terrain') %>
  <%= image_tag(tile.unit.map_image.url, class: 'unit') if tile.unit %>
<% end %>
